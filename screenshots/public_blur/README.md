# Public blurred SQL screenshots

These screenshots are safe public copies for GitHub. User-level identifiers, transaction IDs, device IDs, IP-related values, and final suspicious-user rows are blurred/redacted where needed.
